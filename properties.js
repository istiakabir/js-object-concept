let bottle = {
    name: 'yellow bottle',
    price: '50 TK',
    color: 'yellow',
    isCleaned: true,
};

const keys = Object.keys(bottle);
console.log(keys);

const values = Object.values(bottle);
console.log(values);

const entries = Object.entries(bottle);
console.log(entries);